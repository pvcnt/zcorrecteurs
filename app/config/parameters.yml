parameters:
  database.host:      "%env(DATABASE_HOST)%"
  database.base:      "%env(DATABASE_BASE)%"
  database.username:  "%env(DATABASE_USER)%"
  database.password:  "%env(DATABASE_PASSWORD)%"

  secret: dDj85§fd+dedS9-sE4
  cache_default_lifetime: 600
  analytics_account: ''

  sphinx_host: localhost
  sphinx_port: 5000

  sendgrid_apikey: "%env(SENDGRID_APIKEY)%"